{
	"name": "danandjen",
	"version": "1.0.0",
	"description": "Dan and Jen's Wedding Website",
	"scripts": {
		"sass": "sass sass:css --style compressed --watch",
		"sass:build": "sass sass:css --style compressed",
		"clean": "rimraf css/*.map",
		"build": "npm run clean && npm run sass:build"
	},
	"devDependencies": {
		"rimraf": "^5.0.5",
		"sass": "^1.71.1"
	}
}